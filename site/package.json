{
  "name": "kerala-web-watch-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "predev": "cd ../audit && npm ci && npm run build",
    "dev": "astro dev",
    "prebuild": "cd ../audit && npm ci && npm run build",
    "build": "astro build",
    "prelint": "cd ../audit && npm ci && npm run build",
    "lint": "astro check",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.7.2"
  }
}
